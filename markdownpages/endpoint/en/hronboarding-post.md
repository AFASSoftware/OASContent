With this UpdateConnector you start the Onboarding process for a new employee in AFAS Profit.

If the employee already exists in AFAS Profit, fill in `EmId` with the correct employee number. The flow will then use the existing employee to start the onboarding process. There is no need to match in the workflow anymore. This is important if you want to have the workflow tasks executed automatically.
